[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "psz"
version = "0.68.0"
description = "Encrypted project archive format with multi-language unpackers (Python/PHP/JS/HTML)"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "DarkFox / SlabyLol"}
]
keywords = ["archive", "encryption", "aes", "psz", "secure-archive"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Security :: Cryptography",
    "Topic :: System :: Archiving",
]
dependencies = [
    "cryptography>=42.0.0",
]

[project.scripts]
psz = "psz.cli:main"

[project.urls]
Homepage = "https://github.com/SlabyLol/psz"
Repository = "https://github.com/SlabyLol/psz"

[tool.setuptools.packages.find]
where = ["."]
include = ["psz*"]
